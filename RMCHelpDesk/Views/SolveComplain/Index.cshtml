@model IEnumerable<RMCHelpDesk.Models.SolveComplainViewModel>
@{
    ViewBag.Title = "Index";
}

<h2>Complain List</h2>

<table class="table">
    <tr>
        <th>
            Complain Details
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Department.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Officer.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
